spring.application.name=business-service
server.port=8082

#Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/waterwala_businesses
spring.datasource.username=root
spring.datasource.password=Raju21@Axe
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# JWT Configuration
jwt.secret=mySecretKey
jwt.expiration=86400

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# Feign Client Configuration
# Enable logging for Feign clients (set to DEBUG to see Feign logs)
logging.level.businessservice.client=DEBUG

# Feign timeout configuration (fallback if bean config fails)
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=10000

# Feign compression (optional - improves performance for large payloads)
feign.compression.request.enabled=true
feign.compression.response.enabled=true
feign.compression.request.mime-types=application/json,text/xml
feign.compression.request.min-request-size=2048

##############################Featured#######################################
# Circuit breaker configuration (optional - for Resilience4j)
# feign.circuitbreaker.enabled=true

# Ribbon configuration (if not using Spring Cloud LoadBalancer)
# ribbon.ReadTimeout=10000
# ribbon.ConnectTimeout=5000